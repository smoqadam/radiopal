# On the server, point the scheduler at the published image and pull it:
#   cp .env.example .env   (then edit OWNER), and `docker compose pull && docker compose up -d`
# Locally, leave this unset and use `docker compose up -d --build`.
RADIOPAL_IMAGE=ghcr.io/owner/radiopal-rs:latest
