lead_seconds: 300
tick_seconds: 20


schedules:
  - name: short_stories
    action: static
    lane: duck
    params: { dir: short_stories, select: shuffle }


  - name: short_stories1
    action: static
    lane: duck
    every: "2h"
    params: { dir: short_stories, select: shuffle }