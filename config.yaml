lead_seconds: 300
tick_seconds: 2


schedules:
  - name: short_stories
    action: static
    lane: duck
    lead: 3
    every: "1m"
#    time: "10:0"
    params: { dir: short_stories, select: shuffle }


  - name: testtesttest
    action: static
    lane: duck
    lead: 10
    every: "30s"
    params: { dir: short_stories, select: shuffle }