lead_seconds: 300
tick_seconds: 2


schedules:
  - name: short_stories
    action: static
    lane: duck
    lead: 40
    every: "1m"
#    time: "10:0"
    params: { dir: short_stories, select: shuffle }


#  - name: short_stories1
#    action: static
#    lane: duck
#    every: "2h"
#    params: { dir: short_stories, select: shuffle }