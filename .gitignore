# secrets — never commit GCP keys
creds/
.env
*.env
*.json.key
service-account*.json

# python
.venv/
__pycache__/
*.pyc

# audio content (not code) — keep the repo light
music/
generated/
station_ids/
short_stories
